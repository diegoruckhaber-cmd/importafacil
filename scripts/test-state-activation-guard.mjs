import assert from "node:assert/strict"; import {evaluateStateActivation,STATE_ACTIVATION_APPROVALS} from "../lib/state-activation-guard.ts"; import {STATE_JURISDICTION_REGISTRY} from "../lib/state-jurisdiction-registry.ts"; const active=["AC","AL","AP","AM","BA","CE","DF","ES","GO","MA","MT","MS","MG","PA","PB","PR","PE","PI","RJ","RN","RS","RO","RR","SC","SP","SE"]; assert.deepEqual(evaluateStateActivation().activeUfs,active); const missing=evaluateStateActivation(STATE_JURISDICTION_REGISTRY,STATE_ACTIVATION_APPROVALS.filter(a=>a.uf!=="SE")); assert.equal(missing.status,"blocked"); assert(missing.violations.some(v=>v.includes("SE: homologated_without_activation_approval"))); console.log("Activation guard: OK — SE requires two-key activation");
