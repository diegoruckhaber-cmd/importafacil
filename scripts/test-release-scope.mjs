import assert from "node:assert/strict"; import { getReleaseScope } from "../lib/release-scope.ts"; const release = getReleaseScope(); assert.deepEqual(release.controlledBeta.activeUfs, ["ES", "PR", "PE", "RO", "SC", "SP"]); assert.equal(release.unrestrictedCommercial.status, "blocked_by_state_scope"); assert.equal(release.unrestrictedCommercial.missingUfCount, 21); console.log("Release scope: OK — RO added; national release remains blocked");
