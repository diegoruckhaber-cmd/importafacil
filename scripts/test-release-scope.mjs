import assert from "node:assert/strict"; import {getReleaseScope} from "../lib/release-scope.ts"; const r=getReleaseScope(); assert.deepEqual(r.controlledBeta.activeUfs,["BA","ES","GO","MT","MS","MG","PR","PE","RS","RO","SC","SP"]); assert.equal(r.unrestrictedCommercial.status,"blocked_by_state_scope"); assert.equal(r.unrestrictedCommercial.missingUfCount,15); console.log("Release scope: OK — BA added; national release remains blocked");
