import assert from "node:assert/strict"; import { getReleaseScope } from "../lib/release-scope.ts"; const release = getReleaseScope(); assert.deepEqual(release.controlledBeta.activeUfs, ["ES", "MG", "PR", "PE", "RO", "SC", "SP"]); assert.equal(release.unrestrictedCommercial.status, "blocked_by_state_scope"); assert.equal(release.unrestrictedCommercial.missingUfCount, 20); console.log("Release scope: OK — MG added; national release remains blocked");
