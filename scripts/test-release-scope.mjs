import assert from "node:assert/strict"; import { getReleaseScope } from "../lib/release-scope.ts"; const release = getReleaseScope(); assert.equal(release.controlledBeta.status, "released_with_restrictions"); assert.deepEqual(release.controlledBeta.activeUfs, ["ES", "PR", "PE", "SC", "SP"]); assert.equal(release.unrestrictedCommercial.status, "blocked_by_state_scope"); assert.equal(release.unrestrictedCommercial.missingUfCount, 22); console.log("Release scope: OK — controlled beta includes ES, PR, PE, SC and SP; national release remains blocked");
