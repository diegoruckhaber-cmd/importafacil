import assert from "node:assert/strict"; import { calculateUnifiedImportSimulation } from "../lib/unified-import-simulation.ts"; import { runImportSimulationV2 } from "../lib/simulation-v2.ts"; const item = { itemId: "I", ncm: "32081020", origin: "México", quantity: 10, weightKg: 100, fobUnit: 20, icms: 12, ttd: "none", destination: "commercial_resale" }; const base = { date: "2026-09-12", exchange: 5.5, freight: 100, insurance: 10, items: [item] }; const pr = calculateUnifiedImportSimulation({ ...base, destinationUf: "PR" }); assert.equal(pr.jurisdiction.stateEngine, "GENERAL"); assert.equal(pr.jurisdiction.scope, "general_rate_only"); assert.equal(pr.calculation.items[0].icmsNormalRate, 19.5); assert.equal(pr.calculation.items[0].icmsImportEffectiveRate, 19.5); for (const uf of ["RJ", "RO", "MG", "MS"]) assert.throws(() => calculateUnifiedImportSimulation({ ...base, destinationUf: uf }), /não homologada/i); assert.throws(() => calculateUnifiedImportSimulation({ ...base, destinationUf: "PR", items: [{ ...item, ttd: "409" }] }), /apenas para a regra geral de ICMS/i); for (const uf of ["ES", "PR", "PE", "SC", "SP"]) { const v2 = runImportSimulationV2({ ...base, destinationUf: uf, scenarioName: uf, targetMarginPercent: 20 }); assert.notEqual(v2.status, "blocked"); } console.log("State jurisdiction scope: OK — Paraná general rate active; unsupported UFs fail closed");
