import assert from "node:assert/strict";
import fs from "node:fs";
import { calculateUnifiedImportSimulation } from "../lib/unified-import-simulation.ts";
import { runImportSimulationV2 } from "../lib/simulation-v2.ts";
const item = { itemId: "ITEM-001", name: "Verniz", ncm: "32081020", origin: "México", quantity: 10, weightKg: 100, fobUnit: 20, icms: 12, ttd: "none", destination: "commercial_resale" }; const base = { date: "2026-09-12", exchange: 5.5, freight: 100, insurance: 10, transportMode: "air", declarationType: "di", items: [item] };
const sc = calculateUnifiedImportSimulation({ ...base, destinationUf: "SC" }); assert.equal(sc.jurisdiction.stateEngine, "SC"); assert.deepEqual(sc.jurisdiction.homologatedUfs, ["ES", "PE", "SC", "SP"]);
const sp = calculateUnifiedImportSimulation({ ...base, destinationUf: "SP" }); assert.equal(sp.calculation.items[0].icmsNormalRate, 18);
const es = calculateUnifiedImportSimulation({ ...base, destinationUf: "ES" }); assert.equal(es.calculation.items[0].icmsNormalRate, 17);
const pe = calculateUnifiedImportSimulation({ ...base, destinationUf: "PE" }); assert.equal(pe.jurisdiction.stateEngine, "GENERAL"); assert.equal(pe.jurisdiction.scope, "general_rate_only"); assert.equal(pe.calculation.items[0].icmsNormalRate, 20.5); assert.equal(pe.calculation.items[0].icmsImportEffectiveRate, 20.5); assert.equal(pe.calculation.items[0].importICMSSavings, 0);
for (const uf of ["PR", "RO", "MG", "MS", "RJ"]) assert.throws(() => calculateUnifiedImportSimulation({ ...base, destinationUf: uf }), new RegExp(`UF ${uf} não homologada`, "i")); for (const uf of ["SP", "ES", "PE"]) assert.throws(() => calculateUnifiedImportSimulation({ ...base, destinationUf: uf, items: [{ ...item, ttd: "409" }] }), /apenas para a regra geral de ICMS/i);
for (const uf of ["SC", "SP", "ES", "PE"]) { const v2 = runImportSimulationV2({ ...base, destinationUf: uf, scenarioName: `${uf} homologada`, targetMarginPercent: 20 }); assert.equal(v2.jurisdiction?.destinationUf, uf); assert(v2.summary?.landedCostBrl > 0); assert.notEqual(v2.status, "blocked"); }
const v2Pr = runImportSimulationV2({ ...base, destinationUf: "PR", scenarioName: "PR fora do escopo", targetMarginPercent: 20 }); assert.equal(v2Pr.status, "blocked"); assert.equal(v2Pr.summary, null); assert(v2Pr.issues.some((issue) => issue.code === "state_jurisdiction_unsupported"));
const route = fs.readFileSync("app/api/sc-federal-calculate/route.ts", "utf8"); assert.match(route, /destinationUf/); assert.match(route, /body\.uf/); console.log("State jurisdiction scope: OK — SC full; SP, ES and PE general-rate-only; remaining UFs fail closed");
