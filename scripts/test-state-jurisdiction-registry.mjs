import assert from "node:assert/strict"; import fs from "node:fs"; import { BRAZILIAN_UFS, getStateJurisdictionRegistry, resolveStateJurisdiction } from "../lib/state-jurisdiction-registry.ts"; import { calculateUnifiedImportSimulation } from "../lib/unified-import-simulation.ts";
const registry = getStateJurisdictionRegistry(); assert.equal(BRAZILIAN_UFS.length, 27); assert.deepEqual(registry.homologatedUfs, ["ES", "PR", "PE", "SC", "SP"]); for (const uf of ["ES", "PR", "PE", "SC", "SP"]) assert.equal(resolveStateJurisdiction(uf)?.status, "homologated"); const base = { date: "2026-09-12", exchange: 5.5, items: [{ itemId: "A", ncm: "32081020", origin: "México", quantity: 1, weightKg: 1, fobUnit: 100, icms: 12 }] }; for (const uf of ["ES", "PR", "PE", "SC", "SP"]) assert.doesNotThrow(() => calculateUnifiedImportSimulation({ ...base, destinationUf: uf })); assert.throws(() => calculateUnifiedImportSimulation({ ...base, destinationUf: "RJ" }), /não homologada/i); const implementation = fs.readFileSync("lib/state-jurisdiction-registry.ts", "utf8"); assert.doesNotMatch(implementation, /\b(iiRate|ipiRate|pisRate|cofinsRate|icmsRate)\b\s*:/i); console.log("State jurisdiction registry: OK — ES, PR, PE, SC and SP homologated; remaining UFs fail closed");
