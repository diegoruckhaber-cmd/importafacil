# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Preferred backend-only elevated key (modern format: sb_secret_...)
SUPABASE_SECRET_KEY=
# Legacy fallback only; keep server-side if still in use.
SUPABASE_SERVICE_ROLE_KEY=

# Stripe
# Use server-only credentials that match the target environment.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRO_PRICE_ID=
